import { InvestmentvaluePipe } from './investmentvalue.pipe';

describe('InvestmentvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
